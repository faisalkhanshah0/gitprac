first commit.

second commit.  stash

git reset --hard commitid

git push -f origin master

to show all heads
git reflog 

to show all commit
git log

git diff commit1 commit2


git show commitid

git stash clear

to add specif commit from some other branch
git cherry-pick commitid

git commit --amend -m 'new message'

git reflog
git checkout HEAD@{1}
but it will open these changes as a new branch


extra code to delete
 asdfasdf
